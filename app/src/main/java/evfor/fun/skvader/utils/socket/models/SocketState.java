package evfor.fun.skvader.utils.socket.models;

public class SocketState {
    public boolean isConnected;

    public SocketState(boolean isConnected) {
        this.isConnected = isConnected;
    }
}
